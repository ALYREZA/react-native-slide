{
  "name": "pug-slide",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "npx parcel index.pug",
    "build": "npx parcel build index.pug",
    "now-build": "npx parcel build index.pug"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "pug": "^2.0.4"
  }
}
